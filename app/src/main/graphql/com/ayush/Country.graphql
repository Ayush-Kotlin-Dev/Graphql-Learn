query Country($country_code: ID!) {
    country(
        code : $country_code
    ) {
        code
        name
        continent {

            name
        }
        currency
        languages {
            name
        }
        emoji
        capital

    }

}